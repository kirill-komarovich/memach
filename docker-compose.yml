version: '3.7'
services:
  bot:
    build: .
    command: ruby bin/bot
    depends_on:
      - db
    volumes:
      - .:/memach/
    env_file:
      - .env.development
  db:
    image: postgres:11.5
    volumes:
      - ./db/data/pgdata:/var/lib/postgresql/data/pgdata
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
